<!DOCTYPE html>
<html>
<%- include('partials/head') %>
<body class="auth-body">
  <div class="auth-card">
    <h1>Opstify</h1>
    <p class="muted">PeopleOps request management</p>

    <form method="POST" action="/login">
      <label>
        Your name
        <input type="text" name="name" required autofocus>
      </label>

      <fieldset>
        <legend>Role</legend>
        <label class="radio-option">
          <input type="radio" name="role" value="employee" <%= preselectedRole === 'employee' ? 'checked' : '' %>>
          Employee
        </label>
        <label class="radio-option">
          <input type="radio" name="role" value="admin" <%= preselectedRole === 'admin' ? 'checked' : '' %>>
          PeopleOps admin
        </label>
        <label class="radio-option">
          <input type="radio" name="role" value="field_worker" <%= preselectedRole === 'field_worker' ? 'checked' : '' %>>
          Field worker
        </label>
      </fieldset>

      <label class="optional-fields-start">
        Department <span class="muted">(optional)</span>
        <input type="text" name="department">
      </label>

      <label>
        Job title <span class="muted">(optional)</span>
        <input type="text" name="job_title">
      </label>

      <button type="submit">Continue</button>
    </form>
  </div>
</body>
</html>
